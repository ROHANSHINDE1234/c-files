#include <stdio.h>
#include "codechef.h"
int main()
{
    u5 T = testcases();
    while (T--)
    {
        u5 ipary[1];
        inputarray(ipary,1);
        u1 ipchar[ipary[0]];
        for (u5 i = 0; i < ipary[0]; i++)
        //{
            printf("%s\n",ipchar);
        //}
        
    }
    return 0;
}
